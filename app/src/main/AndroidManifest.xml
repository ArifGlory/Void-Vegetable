<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myproject.avoid">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterAcitivy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginPSayur"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterPSayurActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BerandaActivity"
            android:label="V.O.I.D"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PSayurActivity"
            android:label="Penjual Sayur"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListSayurActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TambahSayurActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PSayur2Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BerandaMotorGoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PenjualSayurActivity"
            android:label="V.O.I.D | Penjual"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListSayurPembeli"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Vegetable Online"></activity>
    </application>

</manifest>